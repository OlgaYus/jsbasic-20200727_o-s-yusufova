/**
 * sum
 * @param {number} m
 * @param {number} n
 * @returns {number}
 */

function sum(m, n) {
  return m + n;
}
/**alert(`1 + 1 = ${sum(1, 1)}.`);
alert(`2 + 5 = ${sum(2, 5)}.`);**/

let result = sum(1, 1);
alert(result); 
let result = sum(2, 5);
alert(result); 
